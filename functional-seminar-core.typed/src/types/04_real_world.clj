(ns types.04-real-world)

;; Circle CI: https://www.youtube.com/watch?v=a0gT0syAXsY#t=1606

;; Short:
;; - Useful for cases which are hard to test using unit-tests
;;   - Integrations
;; - 50k CLOC
;; - 10k CLOC typed clojure
;; - ~600 typed vars
;;   - ~50% of those are checked
;;   - ~30% no-check
;;   - the rest from 3rd party libs
